include_rules

: foreach *.c | *.h |> !cc |>
: fs.o repo/pull.o repo/remote.o log.o |> !ar |> libspm.a
: script/fs.o script.o options.o spm.o log.o ../@(DIR_LUA_ROOT)/src/liblua.a ../@(DIR_LIBGIT2_ROOT)/libgit2.a |> !ld |> spm
