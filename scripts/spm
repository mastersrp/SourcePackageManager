DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export SPM_ROOT=${DIR}/..
export PATH=${SPM_ROOT}/build/bin:$PATH

# Add custom options or settings below, like persistent environment variables.
